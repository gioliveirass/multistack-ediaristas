import { experimentalStyled as styled } from '@material-ui/core';

export const AppContainer = styled('div')`
    display: flex;
    flex-direction: column;
    min-height: 100vh; // Precisa ter pelo menos toda a altura da tela
    
`;